{
  "name": "pooltogether",
  "license": "MIT",
  "version": "3.1.0-beta.1",
  "scripts": {
    "reinstall": "rm -rf node_modules/ && yarn",
    "clean": "rm -rf build/ generated/",
    "prepare:xdai": "mustache networks/xdai.json subgraph.template.yaml subgraph.xdai.yaml",
    "prepare:sokol": "mustache networks/sokol.json subgraph.template.yaml subgraph.sokol.yaml",
   
    "gen:xdai": "graph codegen subgraph.xdai.yaml",
    "gen:sokol": "graph codegen subgraph.sokol.yaml",
   
    "deploy:xdai": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/pooltogether-xdai-v3_3 subgraph.xdai.yaml",
    "deploy:sokol": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/pooltogether-sokol-v3_3 subgraph.sokol.yaml",
      
    "all-sokol": "yarn clean && yarn prepare:sokol && yarn gen:sokol && yarn deploy:sokol",
   
    "all-xdai": "yarn clean && yarn prepare:xdai && yarn gen:xdai && yarn deploy:xdai",
   
    "codegen": "graph codegen",
    "build:xdai": "graph build subgraph.xdai.yaml",
    "build:sokol": "graph build subgraph.sokol.yaml",
    "auth": "graph auth https://api.thegraph.com/deploy/",
    "tag-release": "PACKAGE_VERSION=$(./scripts/package-version.sh) && git tag -ae v$PACKAGE_VERSION && git push --tags"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@pooltogether/pooltogether-contracts": "^3.3.5",
    "mustache": "^4.0.1"
  }
}
