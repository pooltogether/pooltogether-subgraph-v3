{
  "name": "pooltogether",
  "license": "MIT",
  "version": "3.1.0-beta.1",
  "scripts": {
    "reinstall": "rm -rf node_modules/ && yarn",
    "clean": "rm -rf build/ generated/",
    "prepare:polygon": "mustache networks/polygon.json subgraph.template.yaml subgraph.polygon.yaml",
    "prepare:mumbai": "mustache networks/mumbai.json subgraph.template.yaml subgraph.mumbai.yaml",
   
    "gen:polygon": "graph codegen subgraph.polygon.yaml",
    "gen:mumbai": "graph codegen subgraph.mumbai.yaml",
   
    "deploy:polygon": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/pooltogether-polygon-v3_3 subgraph.polygon.yaml",
    "deploy:mumbai": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/pooltogether-mumbai-v3_3 subgraph.mumbai.yaml",
      
    "all-mumbai": "yarn clean && yarn prepare:mumbai && yarn gen:mumbai && yarn deploy:mumbai",
   
    "all-polygon": "yarn clean && yarn prepare:polygon && yarn gen:polygon && yarn deploy:polygon",
   
    "codegen": "graph codegen",
    "build:polygon": "graph build subgraph.polygon.yaml",
    "build:mumbai": "graph build subgraph.mumbai.yaml",
    "auth": "graph auth https://api.thegraph.com/deploy/",
    "tag-release": "PACKAGE_VERSION=$(./scripts/package-version.sh) && git tag -ae v$PACKAGE_VERSION && git push --tags"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@pooltogether/pooltogether-contracts": "^3.3.5",
    "mustache": "^4.0.1"
  }
}
